本程序用于佛山科学技术学院 校园网上网 神州数码 认证客户端，使用者要先安装winpcap, 编译还要下载winpcap的开发包

使用方法：
    1.在Debug目录下，修改data.txt 文件，格式为
        用户名
        密码
        ip地址
        子网掩码
        网关
        本机的MAC地址
        版本号
        网卡序号
    2.双击 Text1.exe

    说明: 版本号，默认不用修改;当发现版本过期时，修改这里就行了。
          网卡序号，就是第几块网卡，你可以用ipconfig /all 查看，或在命令行执行Text1.exe
          Text1.exe 要与 data.txt 在同一个目录

此版本支持变化的心跳包
到 2011/10/24 还可以使用。


