this is digit china client of fosu
佛大校园网神州数码上网认证客户端,2010.06可用,当校园升级时，可能会检查数据包的version,只要加一个参数v就可以用了。

在ubuntu下安装
1.install m4
2.install bison
3.install flex
4.install libpcap
5.install libpcap-dev
6.
  cd 802
  make  
  sudo ./zdclient -u 用户名 -p 密码 -b

fedora 下用gcc重新编译
./configure
make clean
make 
make install
   
